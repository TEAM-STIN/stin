# 아키텍처 및 의사결정 기록

작업하면서 결정한 것들을 그때그때 여기 채워나간다. README를 나중에 몰아 쓰는 대신,
이 파일을 먼저 채우고 README는 여기서 뽑아 정리하는 순서를 권장.

## 시스템 구성

```
[브라우저]
     │ HTTPS
     ▼
┌──────────────── AWS EC2 ────────────────┐
│  Nginx  ─┬─ /      → Next.js  (:3000)   │
│          └─ /api   → NestJS   (:3001)   │
│                          │               │
│                          ▼               │
│                    PostgreSQL (컨테이너) │
└───────────────────────────────────────────┘
```

## 왜 이렇게 결정했나

<!-- 결정마다 한 항목씩 추가. 예:

### 왜 성분 병용 규칙을 개별 성분이 아닌 기능군 단위로 뒀는가
레티놀, AHA, BHA 등은 브랜드마다 세부 성분명이 다르지만 기능적으로는 같은
카테고리로 묶인다. 개별 성분 단위로 규칙을 걸면 성분이 추가될 때마다 규칙을
다시 써야 한다. IngredientGroup 계층을 둬서 새 성분이 추가돼도 기능군만
지정하면 기존 규칙이 자동 적용되게 했다.

### 왜 EC2에 직접 배포했는가
관리형 플랫폼(Vercel 등)을 쓰지 않고 EC2에 Nginx + Docker Compose로 직접
구성했다. 배포 과정 자체를 학습 목표에 포함했기 때문. Next.js는 Vercel이
CDN·이미지 최적화를 대신 해주지만, 그건 재발명이 아니라 이미 있는 것을
가져다 쓰는 것이라 배움이 적다고 판단했다.

-->

## 추천 엔진 처리 흐름

```
1. RoutineTemplate 조회 → 단계 구성 + 시간대
2. 단계별 후보 필터링 (피부타입 · 고민 태그 일치)
3. 조합 생성
4. 성분 병용 규칙 검증 (IngredientRule, BLOCK/WARN)
5. 트레이드오프 처리 (해가 없으면 우선순위대로 조건 완화,
   또는 아침/저녁 분리로 해소 시도)
6. 추천 근거 문장 생성
```

## 데이터 모델

`apps/api/prisma/schema.prisma` 참고.

## 미해결/보류 사항

<!-- 나중에 결정하기로 미룬 것들. 예: 상태관리 라이브러리, CI 파이프라인 등 -->
