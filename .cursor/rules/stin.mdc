---
description: STIN 레포 작업 규칙 진입점
alwaysApply: true
---

이 레포의 작업 규칙 정본은 루트 `AGENTS.md`다. **작업을 시작하기 전에 반드시 읽어라.**

`AGENTS.md`는 목차이며, 거기에서 지시하는 `docs/` 하위 문서를 필요할 때 찾아 읽는다.

이 파일에는 규칙 본문을 복사해 두지 않는다 — 복사하면 반드시 `AGENTS.md`와 갈라진다.
규칙을 바꿀 때는 `AGENTS.md`만 수정한다.
